const config = {
  plugins: {
    'postcss-nesting': {},          // Use the updated nesting plugin
    tailwindcss: {},                // Use the correct Tailwind CSS plugin for PostCSS
    autoprefixer: {},               // Optional: for browser compatibility
  },
};

export default config;
